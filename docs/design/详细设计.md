# DeepCode 详细设计文档(骨架)

> 状态:待细化。本文件先搭骨架,随各模块设计确定后逐节填充。
> 概要设计见 [概要设计.md](./概要设计.md)。
> 最近更新:2026-06-13

本阶段大部分细节尚未确定,以下为待填充的章节结构,标注 TODO 的为尚未敲定的内容。

## 1. 核心编排层(LangGraph)
> 总体结构已定(概要设计 §7 / D11):ReAct 式 tool-calling loop。以下细化字段与实现。
- TODO:State 字段精确类型(messages / cwd / repo_root / retrieved_context / iteration / budget / plan[二期])
- TODO:`agent`、`tool_executor` 节点实现;条件边判定逻辑
- TODO:并行工具调用(D34):`tool_executor` 并发执行多个 tool_call;写权工具(edit/bash)的并发安全/串行化策略
- TODO:checkpoint / 会话恢复(D22):checkpointer 后端选型(内存 / SQLite)、恢复入口(`--resume`/选历史会话)、与 D15 跨会话持久化的存储关系
- TODO:loop 终止条件阈值(最大轮次、token/步数预算具体值)

## 2. 工具层
> 编辑策略已定(D12/D32):精确字符串替换为主,新建/小文件整文件写入兜底,不用 unified diff;失败回灌最相似块/行号让模型重试。
> 输出截断已定(D31):bash/read 等输出做字节/行/token 上限截断 + 续看提示。
- TODO:工具集 schema(grep/glob、read、edit、bash、symbol_outline、find_definition、find_references)
- TODO:edit 匹配失败处理(D32 最小版):未匹配/多处匹配的回灌信息格式与重试上限
- TODO:输出截断(D31)的具体上限值、截断标记格式、续看(offset/grep)接口
- TODO:工具描述(tool description)的 prompt 设计 —— 对 DeepSeek 需写细、给例子

## 7. 执行模型(本地执行,D13 修订)
> 方案已定:默认本地子进程执行 + 权限确认;`Executor` 抽象接口保留(四期插入 OS 级沙箱)。取消 Docker。
- TODO:`Executor` 接口签名(run / 流式输出 / 超时 / 退出码 / cwd)
- TODO:本地子进程管理(超时、kill、环境变量、工作目录)
- TODO:危险命令的用户确认流程(D17:写/执行确认 + 本会话记住/允许列表)
- TODO(四期):OS 级沙箱实现(Landlock/seccomp/Seatbelt)作为 Executor 隔离实现

## 3. 上下文 / 检索层
- TODO:`Retriever` 接口签名(输入/输出数据结构)
- TODO:TextRetriever 实现细节
- TODO:SymbolRetriever 实现(tree-sitter 语法、支持的语言、符号抽取与缓存)
- TODO:repo-map 生成与排序算法(参考 Aider 的图排序思路)
- TODO:`/rag` 命令触发 EmbeddingRetriever 的流程(回灌方式已定:结果并入主消息流,见概要设计 D10)
- TODO(未来增强):自动意图 router(规则/关键词 vs 廉价 LLM 分类)与 hybrid rerank 策略
- TODO(二期):EmbeddingRetriever、向量库选型、Merkle 增量索引、chunk 切分与 hash

## 4. Provider 层
- TODO:Provider 抽象接口(chat、tool-calling、流式)
- TODO:DeepSeek 适配实现(base_url、鉴权、错误重试)
- TODO:function calling 兼容性处理与降级策略
- TODO:推理 token 处理(D35):识别 `reasoning_content`/`<think>`,剥离出消息历史、单独呈现
- TODO:effort 映射(D36):等级 → 推理预算/`reasoning_effort`;不支持预算时的降级策略(行为提示 + 规划深度)
- TODO(编码期):流式输出与中途打断(Ctrl-C)处理

## 5. CLI 层
> 框架已定(D14):Rich(渲染)+ prompt_toolkit(输入)。
- TODO:**slash command 子系统**(通用命令解析,首个命令 `/rag`,可扩展)
- TODO:**`/effort` 命令(D36)**:解析 `[low|medium|high|max|auto]`、设置并持久化等级、回显当前等级
- TODO:流式输出渲染、markdown/代码高亮、权限确认交互的具体交互
- TODO:多行输入、历史、补全的配置

## 6. 横切关注点
> 上下文管理策略已定(D15):滑动窗口 + 自动摘要压缩 + 跨会话持久化。
- TODO:压缩触发阈值、摘要用哪个模型/prompt、保留多少近期原文
- TODO:跨会话持久化的存储格式与位置
> 权限模型已定(D17):只读自动放行,写/执行需确认。TODO:确认 UI 交互、"本会话记住"/允许列表的存储、yolo 模式开关
> 配置/密钥已定(D18):分层配置 + 密钥走环境变量/独立文件不入库。TODO:配置文件 schema、加载优先级、密钥读取顺序
> 环境/依赖已定:用 **uv** 管理虚拟环境与依赖,项目清单为 `pyproject.toml`。TODO:一期创建 `pyproject.toml`(含 dev 依赖组:ruff、pytest)、锁文件 `uv.lock` 纳入版本控制
- TODO:日志与可观测性(尤其用于 function calling 压测)
> 项目规则/记忆文件已定(D25,一期):加载类 CLAUDE.md/AGENTS.md/.cursorrules 注入 system。TODO:文件名约定、分层合并顺序、注入位置与 token 预算

## 8. 扩展机制(一期)

### 8.1 MCP 集成(D21)
> 已定:作为 MCP client 接入外部 server,tools/resources 统一注册进工具层。
- TODO:MCP client 实现(用官方 MCP Python SDK)、server 连接/握手/生命周期
- TODO:MCP 工具 → 内置工具统一接口的适配与命名冲突处理
- TODO:`.deepcode/` 中 MCP server 列表的配置 schema
- TODO:MCP 工具调用的权限确认归类(并入 D17 写/执行确认)

### 8.2 Skill 系统(D20)
> 已定:可复用能力模块(指令/工作流 + 可选专用工具)的加载与调用机制。
- TODO:skill 格式定义(目录结构 / manifest / 指令文件,参考 Claude Code skill)
- TODO:skill 发现与加载(用户级 + 项目级路径)
- TODO:skill 触发方式(模型自主调用 vs slash 命令 vs 两者)
- TODO:skill 与工具/MCP 的关系与边界

## 9. 二期功能(D23/D24/D29,实现方案待一期完成后确定)

仅列待细化项,实现细节一期结束后再定:
- EmbeddingRetriever:向量库选型、chunk 切分、Merkle/chunk-hash 增量索引(§5.5)
- `/rag` 命令(D10 回灌)
- **Architect/Plan 模式(D26,二期首位)**:规划/编辑模型分离、双 provider 配置、规划产物如何驱动执行
- plan 任务分解(D16)
- **Hooks 机制(D29,机制层)**:事件定义(PreToolUse/PostToolUse/Stop/UserPromptSubmit/SessionStart 等)、钩子注册与执行、钩子返回值如何影响 loop(参考 Claude Code);**设计成三期 Git/Lint/Test 自愈的底座**
- **Subagents(D29)**:子 agent 派生(类 AgentTool/Task)、子任务隔离/并行、结果回主上下文
- **图像输入(D27)**:provider 多模态消息、接入视觉模型(DeepSeek-VL 或其他)、CLI 图像输入
- **Web 搜索 + URL fetch(D28)**

## 10. 三期功能(对标成熟 agent,概要见 §13)
- **Git 自动提交 + `/undo` + `/diff`(D29)**:作为内置 post-edit hook 实现在二期 Hooks 底座上(对标 Aider auto_commit)
- **Lint/Test 自愈回环(D29)**:编辑后 hook 触发,失败回灌 agent 自修(对标 Aider auto_commit→lint→reflect)
- **显式上下文管理 `@file`/`/add`/`/drop`(D29)**:独立 slash 命令 + 上下文管理子系统,不与上者整合
- IDE 插件、外部文档索引 `@docs`、自定义 slash 命令、后台任务、会话 rewind

## 11. 四期功能(进一步优化,D33,源自 ref review)
实现待定,优先级低于一~三期:
- **编辑鲁棒性完整版**:fuzzy 多策略匹配、`apply_patch` 式结构化编辑格式可选(对标 Aider `editblock_coder`/`search_replace`、Codex `apply_patch`)
- **repo-map PageRank 排序 + 缓存**(对标 Aider `repomap.py` networkx pagerank);D6 一期为朴素大纲注入,此处升级
- **命令安全策略引擎**:命令解析/规范化 + 危险等级判定,替代粗放的写/执行都问(对标 Codex `execpolicy`)
- **OS 级轻沙箱**:Landlock/seccomp/Seatbelt 作为 `Executor` 隔离实现(对标 Codex `sandboxing`),补上取消 Docker 后的隔离
- **上下文压缩精细化**:分段截断、按类型保留、智能 compact(对标 Codex `compact`/`context_manager`)
- **DeepSeek prompt/context caching** 降本
- **跨会话持久记忆**:学习型记忆(对标 Codex `memories`)
- **自动意图 router + hybrid rerank**(原 D8)
